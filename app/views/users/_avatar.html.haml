= form_for @user, :html => { :multipart => true } do |f|
  -if @user.errors.any?
    = render :partial => 'errors'
  .field
    = f.label :avatar
    = f.file_field :avatar
  .actions
    = f.submit 'Upload my avatar!'