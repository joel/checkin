= semantic_form_for @user, :validate => true  do |f|
  - if @user.errors.any?
    = render :partial => 'errors'
  = f.inputs do
    = f.input :gender, :as => :select, :collection => %w( Mr Mlle Mme )
    = f.input :firstname
    = f.input :lastname
    = f.input :company
    = f.input :phone
    = f.input :phone
    = f.input :twitter
    - if can? :manage, :all
      = f.input :admin
    = f.input :bio
    %button{:class => "btn primary",:id => "edit_account_information_submit"}
      Update Person
